<UserControl x:Class="Barista1.UserControls.TestBase.TestBaseReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Barista1.UserControls.TestBase"
             mc:Ignorable="d" 
             Height="auto" Width="auto" Loaded="Window_Loaded">
	<Grid Background="{DynamicResource Light}" >
		<DataGrid x:Name="grid" Margin="40" AutoGenerateColumns="False" BorderBrush="{DynamicResource Dark}"  BorderThickness="1"
				  Background="{DynamicResource Light}" Foreground="{DynamicResource Dark}" FontFamily="Segoe Print Bold">
			<DataGrid.Columns>
				<DataGridTextColumn x:Name="userName"  Binding="{Binding Login}" IsReadOnly="True" Header="Логин" Width="15*" Visibility="Hidden"/>
				<DataGridTextColumn  Binding="{Binding NameTest}" IsReadOnly="True" Header="Тест" Width="34*"/>
				<DataGridTextColumn Binding="{Binding Date}" IsReadOnly="True"  Header="Дата наприсания" Width="21*"/>
				<DataGridTextColumn Binding="{Binding Mark}" IsReadOnly="True"  Header="Оценка" Width="15*"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
